import React from 'react';

const Card = (props: any) => {
    const { } = props;

    return (
        <div style={{margin:8, padding:8,borderRadius:8,boxShadow:"0px 0px 4px grey"}}>
            
        </div>
    );
};

export default Card;